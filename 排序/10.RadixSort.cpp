//
// Created by 钟东 on 17/11/20.
//

#include <iostream>
#include "swap.h"

using namespace std;

class Solution {
public:
};

int main() {
    Solution s;
    int arr[10];
    int length = sizeof(arr) / sizeof(arr[0]);

    cinArr(arr, length);
    printArray(arr, length);
    printArray(arr, length);

    return 0;
};